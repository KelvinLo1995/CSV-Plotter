<Window x:Class="CSViewPlotTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="CSViewPlotTool" Height="450" Width="800"
        Background="#FF1E1E1E" Foreground="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Side: Controls -->
        <StackPanel Grid.Column="0" Margin="10">
            <GroupBox Header="Controls" Background="#FF2D2D30" Foreground="White" Margin="0,0,0,20">
                <StackPanel>
                    <Button Content="Load CSV" Click="LoadCsvButton_Click" Width="100" Margin="0,5" Background="#FF444444" Foreground="White"/>
                    <Button Content="Export CSV" Click="ExportCsvButton_Click" Width="100" Margin="0,5" Background="#FF444444" Foreground="White"/>
                    <Button Content="Save Plot" Click="SavePlotButton_Click" Width="100" Margin="0,5" Background="#FF444444" Foreground="White"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Axis Selection" Background="#FF2D2D30" Foreground="White">
                <StackPanel>
                    <Label Content="X-Axis:" Foreground="White"/>
                    <ComboBox x:Name="xAxisComboBox" Width="150" Margin="0,5" Background="#FF333333" Foreground="White" 
                              BorderBrush="#FF444444" BorderThickness="1">
                        <ComboBox.Resources>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Background" Value="#FF444444"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Margin" Value="2"/>
                                <Setter Property="Padding" Value="5"/>
                            </Style>
                        </ComboBox.Resources>
                    </ComboBox>
                    <Label Content="Y-Axis:" Foreground="White"/>
                    <ScrollViewer MaxHeight="200" Margin="0,5">
                        <ListBox x:Name="yAxisListBox" SelectionMode="Multiple" Width="150" 
                                 Background="#FF444444" Foreground="White" 
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                    </ScrollViewer>
                    <Button Content="Plot" Click="PlotButton_Click" Width="100" Margin="0,10" Background="#FF444444" Foreground="White"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <!-- Right Side: CSV Data View -->
        <DataGrid x:Name="dataGrid" AutoGenerateColumns="True" Margin="10" Grid.Column="2"
                  Background="#FF2D2D30" Foreground="White" 
                  GridLinesVisibility="None" 
                  AlternatingRowBackground="#FF444444" 
                  RowBackground="#FF2D2D30" 
                  HeadersVisibility="All" SelectionChanged="dataGrid_SelectionChanged">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#FF444444"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>
    </Grid>
</Window>